# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: dashboard.spec.js >> TRA-4 | Cancelar un Reporte >> CA-03 | Confirmar cancelación muestra mensaje de éxito
- Location: tests\dashboard.spec.js:106:3

# Error details

```
Test timeout of 35000ms exceeded.
```

```
Error: locator.click: Test timeout of 35000ms exceeded.
Call log:
  - waiting for getByRole('button', { name: /cancelar/i }).first()

```

# Page snapshot

```yaml
- generic [ref=e2]:
  - generic [ref=e7]:
    - heading "¡Bienvenido de vuelta!" [level=2] [ref=e8]:
      - text: ¡Bienvenido
      - text: de vuelta!
    - paragraph [ref=e9]:
      - text: Para seguir conectado inicia sesión
      - text: con tu cuenta personal
    - button "CREAR CUENTA" [ref=e10] [cursor=pointer]
  - generic [ref=e12]:
    - heading "Iniciar Sesión" [level=1] [ref=e13]
    - generic [ref=e15]:
      - generic: ✉️
      - textbox "Correo electrónico" [ref=e16]
    - generic [ref=e18]:
      - generic: 🔒
      - textbox "Contraseña" [ref=e19]
      - button "👁️" [ref=e20] [cursor=pointer]
    - button "INICIAR SESIÓN" [ref=e21] [cursor=pointer]
    - paragraph [ref=e22]:
      - text: ¿No tienes cuenta?
      - link "Regístrate" [ref=e23] [cursor=pointer]:
        - /url: "#"
```

# Test source

```ts
  11  |  *   tests/capturas/TRA-12_Modulo_Reportes/TRA-17_Filtrar_Reportes/
  12  |  */
  13  | import { test, expect } from '@playwright/test';
  14  | 
  15  | const MOCK_REPORTS_USER = {
  16  |   success: true,
  17  |   reportes: [
  18  |     { id: 'RPT001', direccion: 'Av. Central 45, Col. Centro',    estado: 'Pendiente', fecha: '2026-04-20' },
  19  |     { id: 'RPT002', direccion: 'Calle Norte 12, Fraccionamiento', estado: 'Recibido',  fecha: '2026-04-19' },
  20  |     { id: 'RPT003', direccion: 'Calle Sur 99, Barrio Antiguo',    estado: 'Cancelado', fecha: '2026-04-18' },
  21  |   ]
  22  | };
  23  | 
  24  | const MOCK_REPORTS_ADMIN = {
  25  |   success: true,
  26  |   reportes: [
  27  |     { id: 'RPT001', direccion: 'Av. Central 45, Col. Centro',    estado: 'Pendiente', fecha: '2026-04-20' },
  28  |     { id: 'RPT002', direccion: 'Calle Norte 12, Fraccionamiento', estado: 'Recibido',  fecha: '2026-04-19' },
  29  |     // Cancelado NO debe aparecer en admin
  30  |     { id: 'RPT003', direccion: 'Calle Sur 99, Barrio Antiguo',    estado: 'Cancelado', fecha: '2026-04-18' },
  31  |   ]
  32  | };
  33  | 
  34  | // ─────────────────────────────────────────────────────────────────────────────
  35  | // TRA-3: Ver Historial de Mis Reportes (Usuario)
  36  | // ─────────────────────────────────────────────────────────────────────────────
  37  | test.describe('TRA-3 | Ver Historial de Mis Reportes', () => {
  38  |   const CAPTURE = 'tests/capturas/TRA-12_Modulo_Reportes/TRA-3_Ver_Historial';
  39  | 
  40  |   test.beforeEach(async ({ page }) => {
  41  |     // Interceptar la URL REAL del backend
  42  |     await page.route('http://localhost:3000/api/reports', r =>
  43  |       r.fulfill({ contentType: 'application/json', body: JSON.stringify(MOCK_REPORTS_USER) })
  44  |     );
  45  |     await page.route('http://localhost:3000/api/reports**', r =>
  46  |       r.fulfill({ contentType: 'application/json', body: JSON.stringify(MOCK_REPORTS_USER) })
  47  |     );
  48  |     await page.addInitScript(() => {
  49  |       localStorage.setItem('token', 'mock_token_user');
  50  |       localStorage.setItem('role', 'user');
  51  |     });
  52  |     await page.goto('/dashboard');
  53  |     await page.waitForTimeout(2500);
  54  |   });
  55  | 
  56  |   test('CA-01 | Lista muestra ID, Dirección, Estado y Fecha de cada reporte', async ({ page }) => {
  57  |     await page.screenshot({ path: `${CAPTURE}/01_Lista_Reportes_Personales.png`, fullPage: true });
  58  |   });
  59  | 
  60  |   test('CA-02 | Reportes ordenados descendentemente por fecha más reciente', async ({ page }) => {
  61  |     // El primero debe ser 2026-04-20
  62  |     await expect(page.getByText('2026-04-20').first()).toBeVisible();
  63  |     await page.screenshot({ path: `${CAPTURE}/02_Orden_Descendente_Fecha.png`, fullPage: true });
  64  |   });
  65  | 
  66  |   test('CA-03 | Cada reporte tiene botón Cancelar y botón Información', async ({ page }) => {
  67  |     await expect(page.getByRole('button', { name: /cancelar/i }).first()).toBeVisible();
  68  |     await expect(page.getByRole('link', { name: /información/i }).first()).toBeVisible();
  69  |     await page.screenshot({ path: `${CAPTURE}/03_Botones_Cancelar_Info_Visibles.png`, fullPage: true });
  70  |   });
  71  | });
  72  | 
  73  | // ─────────────────────────────────────────────────────────────────────────────
  74  | // TRA-4: Cancelar un Reporte
  75  | // ─────────────────────────────────────────────────────────────────────────────
  76  | test.describe('TRA-4 | Cancelar un Reporte', () => {
  77  |   const CAPTURE = 'tests/capturas/TRA-12_Modulo_Reportes/TRA-4_Cancelar_Reporte';
  78  | 
  79  |   test.beforeEach(async ({ page }) => {
  80  |     await page.route('http://localhost:3000/api/reports', r =>
  81  |       r.fulfill({ contentType: 'application/json', body: JSON.stringify(MOCK_REPORTS_USER) })
  82  |     );
  83  |     await page.route('http://localhost:3000/api/reports**', r =>
  84  |       r.fulfill({ contentType: 'application/json', body: JSON.stringify(MOCK_REPORTS_USER) })
  85  |     );
  86  |     await page.addInitScript(() => {
  87  |       localStorage.setItem('token', 'mock_token_user');
  88  |       localStorage.setItem('role', 'user');
  89  |     });
  90  |     await page.goto('/dashboard');
  91  |     await page.waitForTimeout(2500);
  92  |   });
  93  | 
  94  |   test('CA-01 | Botón Cancelar deshabilitado para reporte en estado Recibido', async ({ page }) => {
  95  |     // RPT002 es Recibido → su botón Cancelar debe estar disabled
  96  |     await page.screenshot({ path: `${CAPTURE}/01_Boton_Deshabilitado_Si_No_Pendiente.png`, fullPage: true });
  97  |   });
  98  | 
  99  |   test('CA-02 | Al presionar Cancelar aparece modal de confirmación', async ({ page }) => {
  100 |     const cancelarBtn = page.getByRole('button', { name: /cancelar/i }).first();
  101 |     await cancelarBtn.click();
  102 |     await page.waitForTimeout(700);
  103 |     await page.screenshot({ path: `${CAPTURE}/02_Modal_Confirmacion_Visible.png`, fullPage: true });
  104 |   });
  105 | 
  106 |   test('CA-03 | Confirmar cancelación muestra mensaje de éxito', async ({ page }) => {
  107 |     await page.route('http://localhost:3000/api/reports/RPT001/cancel', r =>
  108 |       r.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ success: true }) })
  109 |     );
  110 |     const cancelarBtn = page.getByRole('button', { name: /cancelar/i }).first();
> 111 |     await cancelarBtn.click();
      |                       ^ Error: locator.click: Test timeout of 35000ms exceeded.
  112 |     await page.waitForTimeout(500);
  113 |     await page.getByRole('button', { name: /sí, cancelar/i }).click();
  114 |     await page.waitForTimeout(1500);
  115 |     await page.screenshot({ path: `${CAPTURE}/03_Confirmacion_Cancelado_Exitoso.png`, fullPage: true });
  116 |   });
  117 | });
  118 | 
  119 | // ─────────────────────────────────────────────────────────────────────────────
  120 | // TRA-8: Ver Todos los Reportes (Admin)
  121 | // ─────────────────────────────────────────────────────────────────────────────
  122 | test.describe('TRA-8 | Ver Todos los Reportes (Admin)', () => {
  123 |   const CAPTURE = 'tests/capturas/TRA-12_Modulo_Reportes/TRA-8_Todos_Reportes_Admin';
  124 | 
  125 |   test.beforeEach(async ({ page }) => {
  126 |     await page.route('http://localhost:3000/api/reports/all', r =>
  127 |       r.fulfill({ contentType: 'application/json', body: JSON.stringify(MOCK_REPORTS_ADMIN) })
  128 |     );
  129 |     await page.route('http://localhost:3000/api/reports**', r =>
  130 |       r.fulfill({ contentType: 'application/json', body: JSON.stringify(MOCK_REPORTS_ADMIN) })
  131 |     );
  132 |     await page.addInitScript(() => {
  133 |       localStorage.setItem('token', 'mock_token_admin');
  134 |       localStorage.setItem('role', 'admin');
  135 |     });
  136 |     await page.goto('/dashboard');
  137 |     await page.waitForTimeout(2500);
  138 |   });
  139 | 
  140 |   test('CA-01 | Panel Admin muestra reportes sin los Cancelados', async ({ page }) => {
  141 |     // 'Calle Sur 99' es Cancelado → no debe aparecer
  142 |     await expect(page.getByText('Calle Sur 99')).not.toBeVisible();
  143 |     await page.screenshot({ path: `${CAPTURE}/01_Lista_Sin_Cancelados.png`, fullPage: true });
  144 |   });
  145 | 
  146 |   test('CA-02 | Botón Recibir cambia estado y muestra mensaje de éxito', async ({ page }) => {
  147 |     await page.route('http://localhost:3000/api/reports/RPT001/receive', r =>
  148 |       r.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ success: true }) })
  149 |     );
  150 |     const recibirBtn = page.getByRole('button', { name: /recibir/i }).first();
  151 |     if (await recibirBtn.isVisible()) {
  152 |       await recibirBtn.click();
  153 |       await page.waitForTimeout(1200);
  154 |     }
  155 |     await page.screenshot({ path: `${CAPTURE}/02_Reporte_Marcado_Recibido.png`, fullPage: true });
  156 |   });
  157 | });
  158 | 
  159 | // ─────────────────────────────────────────────────────────────────────────────
  160 | // TRA-17: Filtrar Lista de Reportes (Admin)
  161 | // ─────────────────────────────────────────────────────────────────────────────
  162 | test.describe('TRA-17 | Filtrar Lista de Reportes (Admin)', () => {
  163 |   const CAPTURE = 'tests/capturas/TRA-12_Modulo_Reportes/TRA-17_Filtrar_Reportes';
  164 | 
  165 |   test.beforeEach(async ({ page }) => {
  166 |     await page.route('http://localhost:3000/api/reports**', r =>
  167 |       r.fulfill({ contentType: 'application/json', body: JSON.stringify(MOCK_REPORTS_ADMIN) })
  168 |     );
  169 |     await page.addInitScript(() => {
  170 |       localStorage.setItem('token', 'mock_token_admin');
  171 |       localStorage.setItem('role', 'admin');
  172 |     });
  173 |     await page.goto('/dashboard');
  174 |     await page.waitForTimeout(2500);
  175 |   });
  176 | 
  177 |   test('CA-01 | Sin filtro se muestran todos los reportes (activos)', async ({ page }) => {
  178 |     await page.screenshot({ path: `${CAPTURE}/01_Sin_Filtro_Todos_Activos.png`, fullPage: true });
  179 |   });
  180 | });
  181 | 
```